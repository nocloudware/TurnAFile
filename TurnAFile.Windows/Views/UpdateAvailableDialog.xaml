<Window x:Class="TurnAFile.Windows.Views.UpdateAvailableDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Actualización disponible"
        Width="500" Height="420"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackgroundBrush}"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True">

	<Grid Margin="20">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<!-- Icono y título -->
		<StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
			<TextBlock Text="🆕" FontSize="32" Margin="0,0,12,0" VerticalAlignment="Center"/>
			<StackPanel>
				<TextBlock Text="¡Nueva versión disponible!"
                           FontSize="18"
                           FontWeight="Bold"
                           Foreground="{DynamicResource TextPrimaryBrush}"/>
				<TextBlock x:Name="VersionInfoText"
                           Text="Versión X.X.X"
                           FontSize="13"
                           Foreground="{DynamicResource TextSecondaryBrush}"/>
			</StackPanel>
		</StackPanel>

		<TextBlock Grid.Row="1"
                   Text="Tu versión actual:"
                   FontSize="12"
                   Foreground="{DynamicResource TextSecondaryBrush}"
                   Margin="0,0,0,4"/>
		<TextBlock x:Name="CurrentVersionText"
                   Grid.Row="2"
                   Text="v1.0.0"
                   FontSize="14"
                   FontWeight="SemiBold"
                   Foreground="{DynamicResource TextPrimaryBrush}"
                   Margin="0,0,0,15"/>

		<!-- Notas de la versión -->
		<Border Grid.Row="3"
                Background="{DynamicResource SurfaceBrush}"
                CornerRadius="8"
                BorderThickness="1"
                BorderBrush="{DynamicResource BorderBrush}"
                Padding="12"
                Margin="0,0,0,15">
			<ScrollViewer VerticalScrollBarVisibility="Auto">
				<TextBlock x:Name="ReleaseNotesText"
                           Text="Cargando notas de la versión..."
                           FontSize="11"
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           TextWrapping="Wrap"/>
			</ScrollViewer>
		</Border>

		<!-- Botones -->
		<StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
			<ui:Button x:Name="IgnoreButton"
                       Click="OnIgnoreClick"
                       Appearance="Transparent"
                       Padding="12,8"
                       Margin="0,0,8,0"
                       Content="Ignorar esta versión"/>
			<ui:Button x:Name="RemindLaterButton"
                       Click="OnRemindLaterClick"
                       Appearance="Secondary"
                       Padding="12,8"
                       Margin="0,0,8,0"
                       Content="Recordar después"/>
			<ui:Button x:Name="DownloadButton"
                       Click="OnDownloadClick"
                       Appearance="Primary"
                       Padding="16,8"
                       Content="Descargar"/>
		</StackPanel>
	</Grid>
</Window>